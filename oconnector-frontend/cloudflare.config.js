/**
 * Configuração para deploy no Cloudflare Pages com @cloudflare/next-on-pages
 * 
 * NOTA: @cloudflare/next-on-pages está deprecated.
 * Para Next.js 16+, considere usar OpenNext adapter:
 * https://opennext.js.org/cloudflare
 */

module.exports = {
  // Configuração do Cloudflare Pages
  // Para usar @cloudflare/next-on-pages, você precisa:
  // 1. Instalar: npm install @cloudflare/next-on-pages --save-dev
  // 2. Atualizar next.config.ts para usar o plugin
  // 3. Usar npx @cloudflare/next-on-pages para build
};

