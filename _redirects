# Redirects para Cloudflare Pages

# Redirecionar raiz para index.html (já é padrão, mas garantindo)
/ /index.html 200

# Proteger rotas que requerem autenticação
# (verificação no frontend via JavaScript)

# Fallback para SPA - todas as rotas não encontradas vão para index.html
# Isso permite que o JavaScript gerencie as rotas
/* /index.html 200

